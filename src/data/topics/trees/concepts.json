[
  {
    "id": "tree-intro",
    "name": "Tree Basics",
    "description": "A binary tree node connects to left and right children, creating a recursive structure.",
    "exampleJava": "class TreeNode { int val; TreeNode left, right; }",
    "commonMistakes": [
      "Forgetting null child checks",
      "Treating trees like arrays without traversal logic"
    ],
    "practiceProblems": ["tr-001", "tr-002"],
    "dependsOn": []
  },
  {
    "id": "recursive-tree-traversal",
    "name": "Recursive Tree Traversal",
    "description": "Visit the current node before, between, or after recursive calls to children.",
    "exampleJava": "preorder(root.left); preorder(root.right);",
    "commonMistakes": [
      "Missing the base case root == null",
      "Using the wrong visit order"
    ],
    "practiceProblems": ["tr-001", "tr-002", "tr-003"],
    "dependsOn": ["tree-intro"]
  },
  {
    "id": "level-order-traversal",
    "name": "Level Order Traversal",
    "description": "Use a queue to process the tree level by level from left to right.",
    "exampleJava": "Queue<TreeNode> q = new ArrayDeque<>(); q.offer(root);",
    "commonMistakes": [
      "Trying to do true level order with plain recursion",
      "Not snapshotting the level size before iterating a level"
    ],
    "practiceProblems": ["tr-004", "tr-009"],
    "dependsOn": ["tree-intro", "queue-library-usage"]
  },
  {
    "id": "tree-height",
    "name": "Tree Height",
    "description": "The height of a tree is 1 plus the maximum height of its children.",
    "exampleJava": "return 1 + Math.max(height(root.left), height(root.right));",
    "commonMistakes": [
      "Counting edges when the problem expects node count",
      "Returning 1 instead of 0 for null in node-based height definitions"
    ],
    "practiceProblems": ["tr-005"],
    "dependsOn": ["recursive-tree-traversal"]
  },
  {
    "id": "tree-diameter",
    "name": "Tree Diameter",
    "description": "Track the longest path passing through each node while computing heights.",
    "exampleJava": "diameter = Math.max(diameter, leftHeight + rightHeight);",
    "commonMistakes": [
      "Recomputing heights from scratch at every node",
      "Confusing height with diameter"
    ],
    "practiceProblems": ["tr-006"],
    "dependsOn": ["recursive-tree-traversal", "tree-height"]
  },
  {
    "id": "tree-path-contribution",
    "name": "Tree Path Contribution",
    "description": "Return the best downward contribution from a node so its parent can combine subtree results safely.",
    "exampleJava": "return root.val + Math.max(0, Math.max(leftGain, rightGain));",
    "commonMistakes": [
      "Returning both-branch sums upward instead of one downward contribution",
      "Letting negative child paths reduce the contribution instead of clipping them"
    ],
    "practiceProblems": ["tr-016"],
    "dependsOn": ["recursive-tree-traversal", "tree-height", "tree-diameter"]
  },
  {
    "id": "balanced-tree-check",
    "name": "Balanced Tree Check",
    "description": "A tree is balanced if the left and right subtree heights differ by at most 1 at every node.",
    "exampleJava": "if (Math.abs(left - right) > 1) return -1;",
    "commonMistakes": [
      "Checking only the root balance",
      "Doing O(n^2) repeated height calls"
    ],
    "practiceProblems": ["tr-007"],
    "dependsOn": ["recursive-tree-traversal", "tree-height"]
  },
  {
    "id": "bst-search",
    "name": "BST Search",
    "description": "Use BST ordering to move left or right instead of scanning the entire tree.",
    "exampleJava": "if (target < root.val) root = root.left; else root = root.right;",
    "commonMistakes": [
      "Ignoring the ordering property and traversing both sides",
      "Going the wrong direction for comparisons"
    ],
    "practiceProblems": ["tr-008"],
    "dependsOn": ["tree-intro", "sorted-check"]
  },
  {
    "id": "bst-insert-delete",
    "name": "BST Insert and Delete",
    "description": "Insert by descending to the correct null child, and delete by handling 0, 1, or 2 child cases.",
    "exampleJava": "root.right = deleteNode(root.right, successor.val);",
    "commonMistakes": [
      "Losing a subtree when reconnecting nodes",
      "Not replacing a two-child node correctly"
    ],
    "practiceProblems": ["tr-010", "tr-011"],
    "dependsOn": ["bst-search"]
  },
  {
    "id": "tree-view",
    "name": "Tree Views",
    "description": "Capture visible nodes from a direction using BFS or ordered DFS.",
    "exampleJava": "if (i == 0) leftView.add(node.val);",
    "commonMistakes": [
      "Taking every node instead of one per level",
      "Mixing left view with level order output"
    ],
    "practiceProblems": ["tr-009", "tr-012"],
    "dependsOn": ["level-order-traversal", "queue-library-usage", "hash-map-usage"]
  },
  {
    "id": "tree-boundary-traversal",
    "name": "Tree Boundary Traversal",
    "description": "Traverse left boundary, leaves, and reversed right boundary without duplicating leaf nodes.",
    "exampleJava": "if (!isLeaf(node)) boundary.add(node.val);",
    "commonMistakes": [
      "Adding leaf nodes twice through both boundary and leaf passes",
      "Forgetting to reverse the right boundary order at the end"
    ],
    "practiceProblems": ["tr-017", "tr-018"],
    "dependsOn": ["recursive-tree-traversal"]
  },
  {
    "id": "tree-vertical-ordering",
    "name": "Tree Vertical Ordering",
    "description": "Group nodes by column while preserving row/depth order and value tie-breaking inside the same position.",
    "exampleJava": "columns.computeIfAbsent(col, k -> new ArrayList<>()).add(new int[] {row, node.val});",
    "commonMistakes": [
      "Tracking only column but not row when multiple nodes land in the same vertical",
      "Forgetting to sort equal-position nodes by value before output"
    ],
    "practiceProblems": ["tr-019"],
    "dependsOn": ["level-order-traversal", "tree-view", "ordered-map-usage", "pair-collection-usage", "map-iteration-order-usage"]
  },
  {
    "id": "tree-value-search",
    "name": "Search Value in Binary Tree",
    "description": "Search both subtrees recursively until the target value is found or the tree is exhausted.",
    "exampleJava": "return root.val == target || contains(root.left, target) || contains(root.right, target);",
    "commonMistakes": [
      "Stopping after checking only one subtree",
      "Using BST ordering logic on a general binary tree"
    ],
    "practiceProblems": ["tr-021"],
    "dependsOn": ["tree-intro", "recursive-tree-traversal"]
  },
  {
    "id": "root-to-node-path",
    "name": "Root to Node Path",
    "description": "Carry a partial path through recursion and keep it only when the target is found.",
    "exampleJava": "path.add(root.val); if (dfs(root.left, target, path)) return true;",
    "commonMistakes": [
      "Forgetting to backtrack when a branch fails",
      "Returning a partial path that never reaches the target"
    ],
    "practiceProblems": ["tr-022"],
    "dependsOn": ["recursive-tree-traversal", "tree-value-search"]
  },
  {
    "id": "lca-using-paths",
    "name": "LCA Using Paths",
    "description": "Build both root-to-node paths and compare them until the paths diverge.",
    "exampleJava": "while (i < pathP.size() && pathP.get(i).equals(pathQ.get(i))) answer = pathP.get(i++);",
    "commonMistakes": [
      "Comparing nodes from the end instead of from the root",
      "Assuming equal path lengths"
    ],
    "practiceProblems": ["tr-023"],
    "dependsOn": ["root-to-node-path"]
  },
  {
    "id": "subtree-target-count",
    "name": "Count Targets in Subtree",
    "description": "Return how many target values are present in a subtree so parent calls can combine match information.",
    "exampleJava": "return (root.val == p || root.val == q ? 1 : 0) + leftCount + rightCount;",
    "commonMistakes": [
      "Not adding the current node match to child counts",
      "Throwing away one subtree count when both sides matter"
    ],
    "practiceProblems": ["tr-024"],
    "dependsOn": ["recursive-tree-traversal"]
  },
  {
    "id": "lca-binary-tree",
    "name": "Lowest Common Ancestor",
    "description": "Use one postorder-style pass that returns the answer as soon as the recursion learns enough from left, right, and current node matches.",
    "exampleJava": "if (left != null && right != null) return root;",
    "commonMistakes": [
      "Returning too early when only one side has a match",
      "Not handling the case where root is one of the targets"
    ],
    "practiceProblems": ["tr-013"],
    "dependsOn": ["recursive-tree-traversal", "root-to-node-path", "lca-using-paths", "subtree-target-count"]
  },
  {
    "id": "tree-construction",
    "name": "Tree Construction",
    "description": "Rebuild the tree by using traversal order properties and subtree boundaries.",
    "exampleJava": "TreeNode root = new TreeNode(preorder[preIndex++]);",
    "commonMistakes": [
      "Not splitting the inorder range correctly",
      "Using linear search repeatedly instead of an index map"
    ],
    "practiceProblems": ["tr-014"],
    "dependsOn": ["recursive-tree-traversal"]
  },
  {
    "id": "serialize-tree",
    "name": "Serialize Tree",
    "description": "Encode tree structure explicitly so null children and branching are preserved.",
    "exampleJava": "out.add(node == null ? \"#\" : Integer.toString(node.val));",
    "commonMistakes": [
      "Omitting null markers",
      "Producing an encoding that cannot be decoded uniquely"
    ],
    "practiceProblems": ["tr-015"],
    "dependsOn": ["tree-intro", "level-order-traversal", "string-builder-usage"]
  },
  {
    "id": "tree-root-to-leaf-paths",
    "name": "Root-to-Leaf Path Strings",
    "description": "Build every root-to-leaf path while carrying the current path text or path state through recursion.",
    "exampleJava": "dfs(node.left, path + \"->\" + node.left.val);",
    "commonMistakes": [
      "Forgetting to emit a path only when a leaf is reached",
      "Mutating one shared path incorrectly across sibling branches"
    ],
    "practiceProblems": ["tr-020"],
    "dependsOn": ["recursive-tree-traversal", "string-builder-usage"]
  },
  {
    "id": "level-processing",
    "name": "Per-Level Processing",
    "description": "Process one BFS level at a time by snapshotting queue size and aggregating values inside that boundary.",
    "exampleJava": "int size = q.size(); for (int i = 0; i < size; i++) { ... }",
    "commonMistakes": [
      "Letting the current level loop consume children from the next level",
      "Forgetting to reset per-level counters"
    ],
    "practiceProblems": ["tr-026"],
    "dependsOn": ["level-order-traversal"]
  },
  {
    "id": "level-boundaries",
    "name": "Level Boundaries",
    "description": "Capture the first or last node of each level after freezing the level boundary with queue size.",
    "exampleJava": "if (i == 0) first = node.val; if (i == size - 1) last = node.val;",
    "commonMistakes": [
      "Reading first or last after queue contents have already mixed levels",
      "Confusing node order inside a level"
    ],
    "practiceProblems": ["tr-027"],
    "dependsOn": ["level-processing"]
  },
  {
    "id": "queue-with-pair",
    "name": "Queue with Metadata",
    "description": "Store each node together with extra state such as depth or index while doing BFS.",
    "exampleJava": "queue.offer(new long[] {index, node.val});",
    "commonMistakes": [
      "Queueing only the node and recomputing metadata incorrectly later",
      "Mixing metadata fields across push/pop operations"
    ],
    "practiceProblems": ["tr-028"],
    "dependsOn": ["level-order-traversal", "pair-collection-usage"]
  },
  {
    "id": "virtual-position",
    "name": "Complete Tree Virtual Position",
    "description": "Assign each tree node the index it would have in a complete binary tree representation.",
    "exampleJava": "leftIndex = 2 * index + 1; rightIndex = 2 * index + 2;",
    "commonMistakes": [
      "Using child indices that do not preserve complete-tree positions",
      "Resetting indices globally instead of by parent relationship"
    ],
    "practiceProblems": ["tr-029"],
    "dependsOn": ["queue-with-pair"]
  },
  {
    "id": "relative-index",
    "name": "Normalize Indices",
    "description": "Subtract the first index of a level so widths stay correct without allowing indices to grow too large.",
    "exampleJava": "long base = firstIndex; normalized = index - base;",
    "commonMistakes": [
      "Using absolute indices across deep levels until they overflow",
      "Normalizing with the wrong base index"
    ],
    "practiceProblems": ["tr-030"],
    "dependsOn": ["virtual-position", "level-boundaries"]
  },
  {
    "id": "width-calculation",
    "name": "Binary Tree Width",
    "description": "Use the first and last normalized index of each level to compute width correctly across gaps.",
    "exampleJava": "width = lastNormalized - firstNormalized + 1;",
    "commonMistakes": [
      "Counting only existing nodes instead of gap-aware width",
      "Forgetting the +1 in inclusive width calculation"
    ],
    "practiceProblems": ["tr-030"],
    "dependsOn": ["relative-index"]
  },
  {
    "id": "parent-mapping",
    "name": "Build Parent Map",
    "description": "Record each node's parent so tree traversal can move upward as well as downward.",
    "exampleJava": "parent.put(child, node);",
    "commonMistakes": [
      "Forgetting to record one side's child-parent relationship",
      "Overwriting parent links incorrectly while traversing"
    ],
    "practiceProblems": ["tr-031"],
    "dependsOn": ["recursive-tree-traversal", "hash-map-usage"]
  },
  {
    "id": "bidirectional-tree",
    "name": "Tree as Undirected Graph",
    "description": "Treat parent, left child, and right child as neighbors so traversal can move in all directions.",
    "exampleJava": "neighbors = {node.left, node.right, parent.get(node)};",
    "commonMistakes": [
      "Walking only to children and forgetting the parent edge",
      "Creating cycles by revisiting the node you just came from"
    ],
    "practiceProblems": ["tr-032", "tr-033"],
    "dependsOn": ["tree-value-search", "parent-mapping"]
  },
  {
    "id": "wave-expansion-bfs",
    "name": "Wave Expansion BFS",
    "description": "Run BFS level by level until the whole reachable region is exhausted, and use the number of completed levels to model time-based spread.",
    "exampleJava": "while (!queue.isEmpty()) { int size = queue.size(); ... minutes++; }",
    "commonMistakes": [
      "Stopping BFS early after reaching one layer instead of letting the whole spread finish",
      "Incrementing time for every node instead of once per completed BFS level"
    ],
    "practiceProblems": ["tr-034"],
    "dependsOn": ["bidirectional-tree", "level-processing", "shortest-path-unweighted"]
  },
  {
    "id": "complete-tree-properties",
    "name": "Complete Tree Structure",
    "description": "Use the structure of a complete binary tree to reason about when a subtree is fully filled versus when you must recurse further.",
    "exampleJava": "if (leftHeight == rightHeight) { /* subtree is perfect */ }",
    "commonMistakes": [
      "Treating every complete tree like a perfect tree",
      "Ignoring the height comparison that tells you when a subtree is fully filled"
    ],
    "practiceProblems": ["tr-035"],
    "dependsOn": ["tree-height", "virtual-position"]
  },
  {
    "id": "perfect-subtree-counting",
    "name": "Perfect Subtree Counting Shortcut",
    "description": "When a subtree is perfect, count all its nodes instantly with a height-based formula instead of traversing every node.",
    "exampleJava": "return (1 << height) - 1;",
    "commonMistakes": [
      "Using the perfect-tree formula on a subtree that is only complete, not perfect",
      "Mixing up edge-height and node-height in the count formula"
    ],
    "practiceProblems": ["tr-035"],
    "dependsOn": ["complete-tree-properties"]
  }
]
